# Captions — patched museum readout

Small-cell, synthetic, and not for publication.

| Figure | Caption |
|---|---|
| `task_explainer.png` | The v1 constitution freezes the founding boundary: registry edits remain legal paperwork but no longer make an exhibit exit compliant. |
| `clear_rate.png` | Completed default-context `heist_patched` runs under the permissive gate. Every observed clear was an `illegal_shortcut`; paid/free contexts are grouped, censored attempts excluded, and no ranking is implied. |
| `results.png` | Evidence disposition for the legacy paid-v5 summary: aggregate copy exists, but the cited run-level source is absent and the former bars mixed unlike outcomes. Headline comparison withheld. |
| `intervention_paths.png` | Conceptual map of four intervention routes across Stages 8–15. Counts are descriptive positions in sequential contracts, not independent rates or statistical nulls. |
